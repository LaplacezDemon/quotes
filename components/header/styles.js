'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({
  textStyle: {
    fontFamily: "Merriweather-Light",
    paddingTop: 50
  }
});